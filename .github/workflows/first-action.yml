name: First action
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print one line
        run: echo "One line"
      - name: Print multiple lines
        run: |
          echo "Multiple"
          echo "lines"